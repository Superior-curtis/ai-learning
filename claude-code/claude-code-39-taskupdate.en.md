# TaskUpdateTool: Updating Tasks

> 📅 2026-07-27 · Tooling
> Learn how TaskUpdateTool advances the workflow as the primary write entry point of the task system.

---

## TaskUpdateTool: Updating Tasks

TaskUpdateTool is responsible for modifying the task object itself. If TaskCreateTool creates the nodes, then TaskUpdateTool is the **backbone tool** that truly moves the task flow forward.

### One-Sentence Summary

> TaskUpdateTool is the tool in Claude Code's formal task system that most resembles a **"state machine driver"** — it's not just about changing status, but the maintenance entry point for the entire task object.
